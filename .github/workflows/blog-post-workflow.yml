
name: Latest blog post workflow
on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  update-readme-with-blog:
    name: Update this repo's README with latest blog posts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          feed_list: "YOUR_MEDIUM_RSS_FEED,YOUR_DEVTO_RSS_FEED"
```

### B. Add WakaTime Stats (Coding Activity)

1. Sign up at https://wakatime.com
2. Install WakaTime extension in VS Code
3. Get your WakaTime API key
4. Add to your README:

```markdown
## 📊 Weekly Development Breakdown
